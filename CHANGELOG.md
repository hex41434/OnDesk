# Changelog

Weekly fit notes can live here until the digest job commits its own file.

## Unreleased

- First cut: `fit()` / `rank()`, curated GPUs and ~140 models, Mode A table, Mode B Hub paste, `/about`, `/digest` stub.
